server:
  port: ${WX_SERVER_PORT:3012}

spring:
  application:
    name: dyh-club-wx-dev
  redis:
    database: ${REDIS_DATABASE:1}
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    timeout: 2s

dyh:
  wechat:
    callback-token: ${WECHAT_CALLBACK_TOKEN:}
